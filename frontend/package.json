{
  "name": "changewatch-editor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "VITE_ENTRY=editor vite build && VITE_ENTRY=chart vite build",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/d3": "^7.4.3",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
